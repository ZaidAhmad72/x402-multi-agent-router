# research agent — Algorand TestNet, GoPlausible facilitator
AVM_ADDRESS=
FACILITATOR_URL=https://facilitator.goplausible.xyz
PORT=4001
# MOCK=true uses canned findings instead of a live LLM call.
# The x402 payment path is real either way — MOCK never touches payment.
MOCK=true
# Required only when MOCK=false.
GROQ_API_KEY=
