# weather agent — Algorand TestNet, GoPlausible facilitator
# Deterministic by design — no LLM, live Open-Meteo lookup (with a static
# fallback if that API is unreachable).
AVM_ADDRESS=
FACILITATOR_URL=https://facilitator.goplausible.xyz
PORT=4004
